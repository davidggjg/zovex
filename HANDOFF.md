# ZOVEX · מסמך מסירה

> נכתב 04/09/2026. **קרא את הקובץ הזה במלואו לפני שאתה נוגע במשהו.**
>
> 📘 **`OPERATIONS.md` הוא הבסיס** — מה המערכת, מה רץ בשרת, איפה כל קובץ,
> כל הפקודות, ושיטת העבודה. הקובץ הזה מתאר רק את מה שאנחנו **באמצע**.
> הוא קיים כי השיחה הקודמת גדלה מדי, וכי בלעדיו חוזרים לחקור דברים שכבר
> הופרכו במדידה. כל מספר כאן נמדד. כל השערה שהופרכה מסומנת.

---

## 1 · המערכת

שירות סטרימינג בעברית. אתר + אפליקציית אנדרואיד/טלוויזיה, VOD מגובה טלגרם,
~102 ערוצים חיים.

| | |
|---|---|
| שרת | `root@213.139.78.39` · Ubuntu 22.04 |
| דומיין | `zovex.duckdns.org` |
| ריפואים | `davidggjg/zovex` · `davidggjg/zovex-android` |
| ענף | `claude/hls-relay-schema-port-ll8xiz` — **רק אליו דוחפים** |
| הבוט | `/opt/zovex-bot/main.py` · שירות `zovex-bot` · פורט 8000 |
| האתר | `/opt/zovex-site` · nginx מגיש · API בפרוקסי ל-127.0.0.1:8000 |

**דוד מדבר עברית ומריץ את הפקודות בעצמו.** תן פקודות ממוספרות, אחת אחת,
מוכנות להעתקה. הוא ביקש מפורשות: *"תגידי לי לפי הסדר 1 2 3, ואל תביא לי סתם
זרקי ואני אפילו לא יודע מה להריץ."*

---

## 2 · איך משנים דברים — זה החלק שאסור לפספס

### 2.1 האתר

הבנייה נעשית **אצלך**, לא בשרת. השרת רק מקבל ארכיון.

```bash
cd /home/user/zovex
npm run build:vps                 # ← לא "npm run build"! זה מגדיר base=/ במקום /zovex/
rm -f vps-bot/site.tgz && tar czf vps-bot/site.tgz -C dist .
git add vps-bot/site.tgz && git commit && git push
```

ואז בשרת:

```bash
curl -fsSL "https://raw.githubusercontent.com/davidggjg/zovex/claude/hls-relay-schema-port-ll8xiz/vps-bot/site.tgz?$(date +%s)" -o /tmp/site.tgz \
  && find /opt/zovex-site -mindepth 1 -delete && tar xzf /tmp/site.tgz -C /opt/zovex-site \
  && echo "נפרס: $(ls /opt/zovex-site/assets/index-*.js)"
```

**אין צורך ב-restart של nginx** — קבצים סטטיים.

⚠️ **`vps-bot/site.tgz` הוא מה שבאמת מגיע לשרת.** קומיט של קוד מקור בלבד לא
משנה כלום עבור אף משתמש. אם בנית ולא ארזת — לא פרסת.

⚠️ `vps-bot/site.sh` עושה פריסה **מלאה** כולל שכתוב תצורת nginx, והוא מושך
מענף `claude/vps-bot-deploy` (ישן). לעדכון תוכן — השתמש בפקודה שלמעלה.

**אימות שהתיקון באמת בבנדל:** שמות המשתנים נעלמים במזעור. תבנה פעם עם
ופעם בלי ותשווה גדלים, או תחפש קבועים ששורדים (`8e3`, `.001`).

### 2.2 השרת (main.py)

**`main.py` שבשרת שונה מזה שבריפו.** העותק בריפו ישן. אל תסתמך עליו —
עבדתי פעם מולו וזה עלה בשעות.

התבנית שעובדת: **פאץ' שרק מוסיף בסוף הקובץ.** בלי עוגני regex.

```python
TARGET = pathlib.Path("/opt/zovex-bot/main.py")
DONE_MARK = "שם_ייחודי"          # לאידמפוטנטיות
NEEDED = ["api = FastAPI", ...]  # שמות שהתוספת מסתמכת עליהם — אחרת מסרבים
out = src.rstrip("\n") + "\n" + BLOCK
py_compile.compile(...)          # חייב לעבור לפני כתיבה
shutil.copy2(TARGET, f"{TARGET}.bak-<שם>-<תאריך>")
```

**לדרוס פונקציה קיימת:** מגדירים אותה שוב בסוף הקובץ. פייתון מחפש שמות
גלובליים בזמן הקריאה, ולכן ההגדרה האחרונה מנצחת. **חובה לאמת את החתימה
המדויקת** ב-`NEEDED` — הריפו ל-`_make_media_session` מראה `(dc_id)` והשרת
מראה `(client, dc_id)`, ודריסה עם חתימה שגויה שוברת כל קריאה.

**בדיקת NameError חובה** — קומפילציה לא מגלה אותה. אחרי החלה בארגז חול,
פרוס AST ואמת שכל שם גלובלי בבלוק החדש נפתר. (ראה `test_saved_video_meta.py`.)

**התהליך מול דוד:** דוחפים לענף → הוא מריץ `--check` → אם עבר, מריץ בלי,
ואז `systemctl restart zovex-bot`. לכל פאץ' יש `--undo`.

### 2.3 אפליקציית אנדרואיד

`davidggjg/zovex-android`, אותו שם ענף. הבנייה ב-GitHub Actions; דוד מקבל
קישור מוכן ל-APK.

### 2.4 הגדרות

**מקום האמת היחיד: `/opt/zovex-bot/.env`.** הוא נטען אחרון ודורס
drop-ins של systemd. לאמת תמיד:

```bash
curl -s http://127.0.0.1:8000/stream/tune
```

---

## 3 · הבעיה הפתוחה: תקיעות

**התלונה:** סרט נתקע, בעיקר בדקות הראשונות, בצרורות של 3-7, בערך 10 פעמים
בשעה. **דוד מדווח שהאפליקציה עובדת טוב יותר מהאתר** — וזה התברר כנכון וכבעל
הסבר מדויק.

### 3.1 שרשרת הכשל, מאומתת מקצה לקצה

```
חלון לא חוזר תוך 30 שניות
  → main.py מוותר בכוונה ומסיים את התשובה באמצע
  → הדפדפן מקבל גוף קטוע
  → ExoPlayer באפליקציה מבקש שוב ומתאושש · הדפדפן לא עשה כלום
  → הצופה רואה ספינר
```

השורה ביומן: `חלון X-Y ויתר אחרי 30ש — מסיים את התשובה כדי שהנגן יבקש שוב`.
**נמדד: 20 ויתורים בשעה וחצי.** כולם `TimeoutError` עם הודעה ריקה, כלומר
`asyncio.wait_for` הפנימי ולא הקיר.

הקוד אומר את זה במפורש:
> `# הנגן לא שורד שתיקה ארוכה, אבל כן מתאושש מתשובה קטועה`
> `WINDOW_WALL = float(os.environ.get("STREAM_WINDOW_WALL", "30"))`

**ההנחה נכונה ל-ExoPlayer ושגויה לדפדפן.** זה כל ההבדל.

### 3.2 מה כבר תוקן (04/09, ממתין לפריסה)

`src/components/home/CustomVideoPlayer.jsx` — נגן ה-MP4 טיפל בתקיעה בשורה
אחת (`onWaiting → setLoading(true)`), בלי מאזין שגיאות ובלי שומר-סף, בעוד
שלנגן ה-HLS באותו קובץ יש `armStall`. נוסף שומר-סף: 8 שניות בלי התקדמות **וגם**
פחות מחצי שנייה באפר → קפיצה זעירה שמכריחה בקשת טווח חדשה, פעמיים, ואז
טעינה מחדש מאותה נקודה. גם מאזין `error`.

**8 שניות נגזר מהמדידה:** חלון או חוזר תוך שנייה-שתיים או ננטש ב-30. אין אמצע.

**סטטוס: נבנה, נארז ב-`site.tgz`, נדחף. לא נפרס.** ← הצעד הבא.

### 3.3 מה שהופרך — אל תחזור לשם

| השערה | הראיה |
|---|---|
| הקיר של 30ש' גורם לתשובות הקטועות | 0 קירות מול 20 ויתורים בשעה |
| `BAND_TIMEOUT_LIMIT=3` הוא המחיר | הבריכה כבר מוחלפת 145×/שעה |
| ~2,000 חילופי מפתחות DH בשעה | כל החיבורים ל-DC הביתי. `/media-auth/stats` = אפסים |
| לולאת האירועים חסומה | `loop_lag.py`: חציון 3ms, 0 תקיעות ב-2,367 דגימות |
| בקשה מגודרת (hedge) תעזור | 29/08: 4-10 MB/s → 0.05. בוטל, אבל **חזר** ופעיל היום |
| המשיכה מטלגרם איטית | 0.0-0.1ש' לכל 256KB, בכל עומק, בכל מדידה |
| התצורה נדרסה | `/stream/tune` תואם למסמך במדויק |

### 3.4 מה שנשאר, ולמה

```python
# main.py ~1633
parts = await asyncio.wait_for(
    asyncio.gather(*tasks, return_exceptions=True), timeout=budget)
```

`wait_for` על `gather` **מבטל את כל הרצועות** כשהתקציב נגמר — **כולל אלה
שכבר סיימו בהצלחה.** תחת עומס מקבילי זו עבודה כפולה: נמדדו 274-376 קריאות
`upload.GetFile` שנוסו מחדש בחמש/שתי דקות.

`asyncio.wait` מחזיר `(done, pending)` ו**אינו מבטל** — שומרים את מה שהצליח
ומנסים שוב רק את מה שנתקע. **זה השינוי הבא, והוא בלב מסלול ההזרמה.**

⚠️ **חובה מדידת "לפני" ו"אחרי"** עם `bench.py --runs 30 --label`. הכלי מזהיר
בעצמו: אותה בדיקה נמדדה 9.65 MB/s ואז 0.39 MB/s בהפרש 39 דקות. **מדידה
בודדת היא רעש.**

### 3.5 מספרים אחרונים (04/09 08:58, עומס אמיתי)

```
חיבורים פתוחים   329        (אתמול בערב: 130)
Send exception   677/דקה    (אתמול: 71)
Session started  573/דקה    (אתמול: 106)
GetFile נוסה מחדש 188/דקה
תעבורה יוצאת     35 Mbps
```

הידרדרות של פי 5-9 מול הערב הקודם, בתעבורה דומה. **זה מתיישב עם הממצא
המרכזי ב-`STREAMING_DIAGNOSIS.md`: "restart מחזיר את המערכת לתקינות מלאה —
המערכת מתדרדרת עם הזמן."** הניסוי שיכריע ולא בוצע: למדוד, לעשות restart,
למדוד שוב אחרי 3 דקות. אם המספרים צונחים — **התיקון הוא בדליפה ולא בלוגיקת
החלונות**, וכל 3.4 מיותר.

---

## 4 · כלי האבחון — כולם ב-`/opt/zovex-bot/`, כולם קריאה בלבד

| כלי | מה הוא עונה |
|---|---|
| `whystuck.py --min N` | **הכי שימושי.** מודד עכשיו + מסווג את היומן + מוציא tracebacks + סופר "ויתר" = תקיעות |
| `idle_churn.py` | נפילות חיבור **מול תעבורה** — מסרב להסיק אם לא הייתה מנוחה |
| `loop_lag.py` | האם הלולאה חסומה. **כבר ענה: לא** |
| `bench.py --runs 30 --label X` | **אחוז תקיעות**, לא מהירות. המדד היחיד שקובע |
| `stream_probe.py` | משחזר "נתקע באמצע" — מושך בקצב של נגן |
| `capacity.py` | כמה צופים במקביל, ומי נשבר ראשון. **שאלה פתוחה שלא נסגרה** |
| `concurrency_test.py` | עומק / גודל חלון / מקביליות |

**מלכודת ג' מ-`STREAMING_DIAGNOSIS.md`:** מדידה מלקוח חיצוני מוגבלת ברוחב
הפס של הלקוח. **תמיד למדוד מ-`127.0.0.1`.** נפלתי בזה פעמיים.

---

## 5 · הקול בונסדיי — מוכן, לא הורץ

**נמדד:** 16/16 פרקי ונסדיי עם אודיו `ec-3` (Dolby Digital Plus, 5.1).
**שום דפדפן לא מפענח ec-3** — התמיכה מושבתת ב-Chromium מסיבות רישוי. הוא זורק
את הרצועה בשקט: וידאו בלי קול, בלי שגיאה. בטלגרם וב-VLC כן שומעים.

16 פריטים אחרים בקטלוג נדגמו — כולם `mp4a` תקין. זו האצווה של 03/09 בלבד.

**דוד סירב במפורש להורדה+העלאה מחדש** ("זה חונק את השרת"). צדק: 26GB הורדה
+ 24GB העלאה. הפתרון שנבנה במקום הוא Direct Stream, כמו ב-Jellyfin/Plex:

`vps-bot/add_vodfix.py` — פאץ' מוסיף-בלבד, שלושה מסלולים:

- **`/fs/<chat>/<msg>`** — אותו קובץ עם `moov` בהתחלה. ה-moov נמשך פעם אחת
  (2MB), ההיסטים מתוקנים **בזיכרון**, מוגש ככותרת לפני הגוף. תומך בטווחי
  בייטים. **אפס המרה.** מתקן טעינה של 12 שניות וקפיצה תקועה ל-18% מהקטלוג.
- **`/vh/<chat>/<msg>/index.m3u8`** — HLS שלם מפריימי המפתח שב-moov (284
  סגמנטים לפרק, כולם על keyframe). סגמנט מומר לפי דרישה: `-c:v copy -c:a aac`.
- **`/vodinfo/<chat>/<msg>`** — אומר לאתר איזה קישור לנגן.

**אומת מקצה לקצה על הקובץ האמיתי:** 174,892 היסטים תוקנו, גודל הקובץ החדש
זהה לבייט למקורי, ffprobe קרא `avc1 1920x1080` ואורך 3647.552ש', פענח, וקפץ
לדקה 0:20. **מה שלא אומת: הסגמנטים עצמם** — ה-ffmpeg בסביבת הפיתוח קורס
בקריאת כל קובץ TS, כולל כזה שהוא עצמו ייצר. זו בדיקה שחייבת לרוץ בשרת.

**האתר כבר יודע HLS** — נגן Shaka נכנס לכל `.m3u8`. לא צריך לשנות נגן.
**נשאר:** לחבר את `/vodinfo` לבחירת הקישור.

**ההרצה (לא בוצעה):**
```bash
cd /root && curl -fsSL -o add_vodfix.py "https://raw.githubusercontent.com/davidggjg/zovex/claude/hls-relay-schema-port-ll8xiz/vps-bot/add_vodfix.py?$(date +%s)" && python3 add_vodfix.py --check
python3 add_vodfix.py && systemctl restart zovex-bot
curl -s "http://127.0.0.1:8000/vodinfo/-1003936100530/8967"
curl -s "http://127.0.0.1:8000/vh/-1003936100530/8967/s5.ts" -o /tmp/s5.ts && ffprobe -v error -show_entries stream=codec_name,codec_type,channels -of default=nw=1 /tmp/s5.ts
```
צריך לצאת `h264` + `aac` **stereo**.

---

## 6 · חוב שצריך לסגור

| | |
|---|---|
| **`add_authkey_cache.py` מוחל ומיותר** | ההשערה הופרכה, הוא no-op. **להסיר יחד עם התיקון הבא**, בהפעלה מחדש אחת: `python3 add_authkey_cache.py --undo` |
| **`edge_cache` = 16GB** | המגבלה בקוד 3GB ואין דריסה ב-`.env`. **הפינוי לא רץ.** 879 קבצים, הישן מ-12/08 |
| **דיסק** | 44GB/99GB. ~14GB מיותרים: `syslog` 5.7GB (כפילות של journald), core dump 2.3GB, journal 3.9GB, גיבויי קטלוג 588MB |
| **קריסה** | `03/09 18:45:35 code=dumped ABRT`, 2.3GB בזיכרון. אחת בשבוע — לא מסביר תקיעות שקורות כל דקות, אבל לא נחקרה |
| **`fix_saved_workers.py`** | לא הורץ מעולם |
| **באג 416** | בקשת טווח מעבר לסוף הקובץ מחזירה הצלחה עם 0 בייטים במקום 416 |
| **`capacity.py`** | דוד שאל "כמה אנשים יכולים לצפות ביחד". לא נענה |

---

## 7 · מגבלות שאסור לחצות

המערכת מפיצה סרטים וערוצים ברובם ללא רישיון.

- **לא לחזק אנונימיות** או להסתיר זהות מרשויות ומבעלי זכויות.
- **לא לחלץ זרמים מרותי תשלום** של שירותים אחרים, **ולא למפות איפה הם יושבים**.
- **לא להשתמש במוזיקה או בפוסטרים מוגנים** בחומרי שיווק. נוצרה מוזיקה מקורית
  ועיצוב מקורי במקום.
- **אין לדוד מפתחות SSH.** אסור לכבות אימות בסיסמה — זה ינתק אותו מהשרת.
- **לא לדחוף לענף אחר** מ-`claude/hls-relay-schema-port-ll8xiz`.
- כתובת המייל שלו משמשת **רק** לייחוס קומיטים.
- סריקת ערוצי IPTV אושרה מפורשות על ידי בעל הספק.

---

## 8 · לקחים על אופן העבודה

1. **מדוד לפני שאתה קובע.** בשיחה הקודמת הופרכו ארבע השערות שלי ברצף, כל אחת
   במדידה. כל אחת נשמעה משכנעת.
2. **תגיד "טעיתי" ותמשיך.** בלי התנצלויות ארוכות.
3. **כלי שמסרב להסיק שווה יותר מכלי שנותן מספר.** `idle_churn` סירב להסיק
   כשלא הייתה מנוחה, וזה מנע מסקנה שגויה.
4. **קרא את הקוד שבשרת, לא שבריפו.** בקש מדוד `sed -n 'A,Bp'`.
5. **בדוק את הכלים שלך.** `bench.py` קרס ב-`NameError` בכל ריצה; הבודק
   הסטטי שלי עצמו נתן שלוש אזהרות שווא כי לא הכיר closures.
6. **פקודה אחת בכל פעם, ממוספרת.** ותגיד מראש מה כל תוצאה אפשרית תלמד.
